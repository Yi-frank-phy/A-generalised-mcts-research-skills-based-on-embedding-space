# Next-generation proper-volume controller

This branch develops the isolated `dte_nextgen` proper-volume controller. Production `src/dte_backend/**` remains unchanged until the separate evidence gate is satisfied.
