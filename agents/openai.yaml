interface:
  display_name: "DTE Extreme Research"
  short_description: "Run research through the backend-controlled DTE workflow."
  default_prompt: "Use $dte-extreme-research and the production entrypoint strict-run --mode real; keep DTE as the only outer controller."
