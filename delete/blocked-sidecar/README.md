# Blocked sidecar archive

These files are obsolete copies of the former experimental `dte_nextgen` controller. Production `new` uses `src/dte_backend/**` only. Direct deletion of some originals was intermittently blocked by the tool safety layer, so this folder is a review quarantine rather than runnable code.

After manual review, delete the corresponding originals under `src/dte_nextgen/thought_space/` if they still exist, then delete this archive.
